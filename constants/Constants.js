const FAVORITES_KEY = "favorites";
const DATA_KEY = "data";
const SAVE_TO_FAVORITES = "save_to_favorites";
const REMOVE_FROM_FAVORITES = "remove_from_favorites";

const STATE_LOADING = 1;
const STATE_ERROR = 2;
const STATE_SUCCESS = 3;
const STATE_NORMAL = 4;

export default {
    FAVORITES_KEY,
    DATA_KEY,
    SAVE_TO_FAVORITES,
    REMOVE_FROM_FAVORITES,

    STATE_LOADING,
    STATE_ERROR,
    STATE_SUCCESS,
    STATE_NORMAL
};